import * as React from "react";
import NavigatorComponent from "./components/navigator/navigator.component";

const App: React.FC = () => {
  return (
      <NavigatorComponent />
  );
};

export default App;
